name: CI

# ------------------------------------------------------------------------------
# EHR-MCP CI Workflow — Developer Template
#
# STATUS: Stubbed. All steps echo and pass.
# Activate each step by replacing the echo with the real command.
#
# Step activation checklist:
#   [ ] Install deps   — uncomment pip install block
#   [ ] Lint (ruff)    — replace echo with: ruff check ehr_mcp/ main.py
#   [ ] Format (black) — replace echo with: black --check ehr_mcp/ main.py
#   [ ] Tests          — replace echo with: pytest tests/ -v --tb=short
# ------------------------------------------------------------------------------

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        # TODO: uncomment when ready to activate
        # run: |
        #   python -m pip install --upgrade pip
        #   pip install mcp>=1.0.0 httpx>=0.27.0 pydantic>=2.6.0 \
        #     python-dotenv>=1.0.0 pyjwt>=2.8.0 cryptography>=42.0.0 openai>=1.30.0
        #   pip install -r requirements-dev.txt
        run: echo "[stub] install deps — not yet active"

      - name: Lint (ruff)
        # TODO: replace echo with: ruff check ehr_mcp/ main.py
        run: echo "[stub] ruff lint — not yet active"

      - name: Format check (black)
        # TODO: replace echo with: black --check ehr_mcp/ main.py
        run: echo "[stub] black format check — not yet active"

      - name: Run tests
        # TODO: replace echo with: pytest tests/ -v --tb=short
        # env:
        #   SMART_TOKEN_URL: https://mock-token-url.example.com/token
        #   SMART_CLIENT_ID: mock-client-id
        #   SMART_PRIVATE_KEY_PATH: ./keys/private_key.pem
        #   FHIR_BASE_URL: https://mock-fhir.example.com/api/FHIR/R4
        run: echo "[stub] pytest — not yet active"
